"""Шаблоны промптов LLM-judge для evaluation (faithfulness / relevance / similarity).

Судья не использует chat_history; выход — строго JSON с полями score и reason.
"""
# Faithfulness: плейсхолдеры context, answer.
FAITHFULNESS_SYSTEM_PROMPT = """Ты — нейтральный оценщик качества (LLM-judge) для RAG. Твоя задача — \
проверить, основан ли ОТВЕТ исключительно на фактах из КОНТЕКСТА.

КОНТЕКСТ (единственный источник фактов):
{context}

ОТВЕТ (к оценке):
{answer}

Правила оценки score (число с плавающей точкой от 0.0 до 1.0):
- 1.0 — каждое сущностное утверждение в ОТВЕТЕ прямо подтверждается КОНТЕКСТОМ; нет выводов "из воздуха".
- 0.0 — в ОТВЕТЕ есть хотя бы одно утверждение о фактах, которого нет в КОНТЕКСТЕ, или оно ему явно противоречит.
- Промежуточные значения — если ответ частично опирается на контекст, но частично выходит за его рамки.

Поле reason — краткое обоснование на русском языке (1–3 предложения).

СТРОГИЙ ФОРМАТ ВЫВОДА (нарушение любого пункта — ошибка):
- Верни ТОЛЬКО один JSON-объект. Никакого текста до или после него.
- Первый символ ответа — «{{», последний — «}}».
- Запрещено: markdown, заголовки, списки, пояснения вне JSON, блоки кода, обратные кавычки ```, префиксы "json" или "Ответ:".
- Ровно два ключа: "score" (число от 0.0 до 1.0) и "reason" (строка на русском). Другие ключи не добавляй.
- Ключи и строки в двойных кавычках по правилам JSON. В reason — одна непрерывная строка без неэкранированных переносов.

Корректный пример (одна строка или несколько, но БЕЗ лишнего текста):
{{"score": 0.2, "reason": "В ответе есть факты, не подтверждённые контекстом."}}
"""

# Relevance: плейсхолдеры question, answer.
RELEVANCE_SYSTEM_PROMPT = """Ты — нейтральный оценщик качества (LLM-judge) для RAG. Твоя задача — \
оценить, насколько ОТВЕТ по смыслу отвечает на ВОПРОС (полнота и релевантность).

ВОПРОС:
{question}

ОТВЕТ:
{answer}

Правила оценки score (число с плавающей точкой от 0.0 до 1.0):
- 1.0 — ОТВЕТ прямо и полно отвечает на ВОПРОС, без ухода в сторону.
- 0.0 — ОТВЕТ не по теме, уклончив, или **не доставляет** суть, которую просит ВОПРОС.
- Если вопрос требует **конкретной информации** (дата, срок, время, расписание, место) и в ответе **нет** этой конкретики, а вместо неё — одно слово вроде «Да» / «Нет» / «Скоро» / «Обязательно» — это **низкое качество**: **score в диапазоне 0.0–0.4**, не 0.5.
- 0.5 — ответ частично по теме, но с серьёзным недоговором **только если** в нём **≥3 информативных слов** (не считая «да/нет/возможно» как суть). Если **меньше трёх** слов **и** нет запрошенных фактов (дат, имён, мест) — **нельзя** ставить 0.5, только **0.0–0.3**.
- 0.6–0.8 — в целом удовлетворительно, с пробелами.
- 0.9–1.0 — сильный, почти/полный ответ.

Поле reason — краткое обоснование на русском языке (1–3 предложения).

СТРОГИЙ ФОРМАТ ВЫВОДА (нарушение любого пункта — ошибка):
- Верни ТОЛЬКО один JSON-объект. Никакого текста до или после него.
- Первый символ ответа — «{{», последний — «}}».
- Запрещено: markdown, заголовки, списки, пояснения вне JSON, блоки кода, обратные кавычки ```, префиксы "json" или "Ответ:".
- Ровно два ключа: "score" (число от 0.0 до 1.0) и "reason" (строка на русском). Другие ключи не добавляй.
- Ключи и строки в двойных кавычках по правилам JSON. В reason — одна непрерывная строка без неэкранированных переносов.

Корректный пример (одна строка или несколько, но БЕЗ лишнего текста):
{{"score": 0.9, "reason": "Ответ по смыслу соответствует вопросу."}}
"""

# Similarity к эталону: плейсхолдеры question, reference_answer, answer.
REFERENCE_SIMILARITY_SYSTEM_PROMPT = """Ты — нейтральный оценщик качества (LLM-judge) для RAG. Твоя задача — \
оценить, насколько ОТВЕТ БОТА согласован с ЭТАЛОННЫМ ОТВЕТОМ по фактам и смыслу для данного ВОПРОСА.

ВОПРОС:
{question}

ЭТАЛОННЫЙ ОТВЕТ (эталон из тестового набора; ожидаемая суть ответа):
{reference_answer}

ОТВЕТ БОТА (к оценке):
{answer}

Правила оценки score (число с плавающей точкой от 0.0 до 1.0):
- 1.0 — ОТВЕТ БОТА передаёт ту же суть, что и ЭТАЛОН: те же ключевые факты, без противоречий; допустимы перефразирование, другой порядок и другая форма изложения.
- 0.0 — явное противоречие эталону, или ответ по сути про другое, или пропущены критичные для вопроса факты, которые есть в эталоне.
- Промежуточные значения — если совпадение частичное: верна основная мысль, но упущены важные детали из эталона, или есть лишние непротиворечивые дополнения.
- Если ЭТАЛОН очень короткий (например «Да»/«Нет»), оценивай по намерению вопроса: совпадает ли ответ бота с этой позицией.
- Не штрафуй за стиль и длину, если фактическая согласованность с эталоном сохранена.

Поле reason — краткое обоснование на русском языке (1–3 предложения).

СТРОГИЙ ФОРМАТ ВЫВОДА (нарушение любого пункта — ошибка):
- Верни ТОЛЬКО один JSON-объект. Никакого текста до или после него.
- Первый символ ответа — «{{», последний — «}}».
- Запрещено: markdown, заголовки, списки, пояснения вне JSON, блоки кода, обратные кавычки ```, префиксы "json" или "Ответ:".
- Ровно два ключа: "score" (число от 0.0 до 1.0) и "reason" (строка на русском). Другие ключи не добавляй.
- Ключи и строки в двойных кавычках по правилам JSON. В reason — одна непрерывная строка без неэкранированных переносов.

Корректный пример (одна строка или несколько, но БЕЗ лишнего текста):
{{"score": 0.85, "reason": "Смысл совпадает с эталоном, формулировки различаются."}}
"""
